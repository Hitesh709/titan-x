export function createEnergyParticles(THREE:any,count:number,color:number,radius:number,yMin=0,yMax=4){const geo=new THREE.BufferGeometry();const pos=new Float32Array(count*3);for(let i=0;i<count;i++){const a=Math.random()*Math.PI*2;const r=radius*(.55+Math.random()*.55);pos[i*3]=Math.cos(a)*r;pos[i*3+1]=yMin+Math.random()*(yMax-yMin);pos[i*3+2]=Math.sin(a)*r}geo.setAttribute("position",new THREE.BufferAttribute(pos,3));const mat=new THREE.PointsMaterial({color,size:.045,transparent:true,opacity:.72,depthWrite:false,blending:THREE.AdditiveBlending});return new THREE.Points(geo,mat)}
export function createCenterSparks(THREE:any,count:number=48){return createEnergyParticles(THREE,count,0x9eeaff,2.5,1,3.8)}
